<%= form_for(@description) do |f| %>
  <% if @description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@description.errors.count, "error") %> prohibited this description from being saved:</h2>

      <ul>
      <% @description.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :descriptionable_type %><br />
    <%= f.text_field :descriptionable_type %>
  </div>
  <div class="field">
    <%= f.label :descriptionable_id %><br />
    <%= f.text_field :descriptionable_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
