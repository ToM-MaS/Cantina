<h1>Cantina</h1>

<p>This is a very basic web interface to the Cantina Phone Provisioning System.</p>

<% if @popular_manufacturers.count == 0 %>
<h1>Empty database</h1>
<p>
There are no Phone Manufacturers in the database. You probably want to feed the database with the default seeds. To do so go to your command line and stop the rails server with CRTL-C than run the following commands: 
</p>
<pre>
rake db:seed
rails server
</pre>
<% end %>

<h2>Phone Manufacturers</h2>
<ul>
		<% if @popular_manufacturers.count != 0 %>
    		<li><%= link_to "List all manufacturers (#{@number_of_manufacturers})", manufacturers_path %>
			<br />
			Popular manufacturers: 
			<% @popular_manufacturers.each do |manufacturer| %>
			  <%= link_to manufacturer.name, manufacturer %>
			<% end %>
		<% end %>
	</li>	
	<li><%= link_to "Create a new manufacturer", new_manufacturer_path%></li>	
</ul>

<h2>Phone Models</h2>
<ul>
		<% if @popular_phones_models.count != 0 %>
			<li><%= link_to "List all phone models (#{@number_of_phone_models})", phone_models_path %><br />
			Popular phones: 
			<% @popular_phones_models.each do |phone_model| %>
			  <%= link_to phone_model.name, phone_model %>
			<% end %>
		<% end %>
		</li>	
    	<li><%= link_to "Create a new phone model", new_phone_model_path%></li>	
</ul>

<h2>Phones</h2>
<ul>
	<% if @number_of_phones != 0 %>
	  <li><%= link_to "List all phones (#{@number_of_phones})", phones_path %></li>
	<% end %>	
	<li><%= link_to "Create a new phone", new_phone_path %></li>	
</ul>
